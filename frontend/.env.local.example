# Next.js Frontend Environment Variables (Local Development)
# Copy this file to .env.local and use actual values

# Django API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# FastAPI Chat Service Configuration (for AI Stylist)
# This connects to the backend API that handles chat with OpenAI
NEXT_PUBLIC_CHAT_API_URL=http://localhost:8000

# Google OAuth (must match backend configuration)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id-here

# NOTE: OPENAI_API_KEY is configured in backend/.env (server-side only)
# The frontend communicates with the backend API, which handles OpenAI calls securely
